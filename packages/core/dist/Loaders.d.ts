export declare const DEFAULT_LOADERS: ({
    test: string;
    use: {
        loader: string;
    }[];
} | {
    test: string;
    use: {
        loader: string;
        options: {
            pythonExecutable: string;
        };
    }[];
})[];
